namespace SquareCalc;

public static partial class SquareCalculator
{
    public static class Circle
    {
        public static double FromRadius(double radius) => Math.PI * radius * radius;
    }
}
